export class Product {
  // id: string;
  // name: string;
  // description: string;
  // price: number;

  constructor(
    public id: string,
    public name: string,
    public description: string,
    public price: number,
  ) {}
  //TODO: Update with
}
